# Supabase local development files
# These files and directories are generated by the Supabase CLI and are not meant to be in version control.
# They are specific to the local development environment.

# The .branches directory is used by Supabase to manage local database branches.
.branches

# The .temp directory is used by Supabase for temporary files.
.temp

# dotenvx files for managing environment variables.
# These files may contain sensitive information and should not be committed to the repository.
.env.keys
.env.local
.env.*.local
