// LEGACY FILE - REMOVED
// This file conflicted with the working onboarding implementation.
// The working implementation is in:
// - src/contexts/onboarding-context.tsx (React Context & hooks)
// - src/types/onboarding.ts (TypeScript types)
//
// Use the working implementation instead:
// import { useOnboarding } from '@/contexts/onboarding-context'

export {} // Keep as module to avoid import errors// LEGACY FILE - REMOVED
// This file conflicted with the working onboarding implementation.
// The working implementation is in:
// - src/contexts/onboarding-context.tsx (React Context & hooks)
// - src/types/onboarding.ts (TypeScript types)
//
// Use the working implementation instead:
// import { useOnboarding } from '@/contexts/onboarding-context'

export {} // Keep as module to avoid import errors